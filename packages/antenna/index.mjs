// Antenna dimensions, radiation patterns, and the take-off angle a path needs.
//
// Pure ESM with no dependencies, so the browser can import the same file the
// tests run against. See physics.mjs for what the model does and — more
// importantly — what it does not.

export * from './physics.mjs';
export * from './catalogue.mjs';
