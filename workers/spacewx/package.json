{
  "name": "@hfkit/spacewx-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: refreshes the space weather bundle hourly into KV and serves it. The mothership \u2014 clients never contact upstream.",
  "scripts": {
    "deploy": "npx --yes wrangler@4 deploy",
    "dev": "npx --yes wrangler@4 dev",
    "tail": "npx --yes wrangler@4 tail",
    "kv:create": "npx --yes wrangler@4 kv namespace create SPACEWX"
  }
}
